.cards
  width 100%

  ul
    width 100%

    li
      stack()
      margin-bottom 2%
      +above(m)
        unstack()
        column(1/2, $cycle: 2)
      +above(l)
        column(1/3, $cycle: 3)
      +above(xl)
        column(1/4, $cycle: 4)